# This folder is for shared resources with both the Client and the Server Lua files.

# Helper Functions
Any helper functions added to shared/functions.lua can be utilized on both server and client side code.